This folder contains two sql files
The zoo(input) file is the original sql file that is imported to the database
it only has one zoo director and three departments as original data.
The zoo(exported) file is the sql file exported from the database with some 
inputed data that is added/updated through the functionality from our website.